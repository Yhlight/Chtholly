# Chtholly for VS Code

This extension provides `.cns` and `.cfdl` language registration, syntax
highlighting, and the Chtholly language server. Set `chtholly.server.path` when
`chtholly-lsp` is not available on `PATH`.
