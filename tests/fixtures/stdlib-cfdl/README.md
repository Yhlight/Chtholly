# Standard CFDL Fixture

This fixture is the smallest maintained standard-library binding boundary.
`session.cfdl` publishes an opaque resource, an event endpoint, and an
obligation discharge. `main.cns` consumes the module through a local alias;
the alias does not alter the provider identity in the Interop artifact.
