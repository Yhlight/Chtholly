module std::env;

// Arguments are immutable UTF-8 strings owned by the process runtime. An
// out-of-range access is an unrecoverable bounds failure.
pub fn arg_count(): usize;
pub fn arg(index: usize): string;
