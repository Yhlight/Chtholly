module plugin;

// Alpha's scalar process contract is intentionally distinct from beta's.
pub fn process(value: i32): i32 {
  return value + 1;
}

pub fn hold(iterations: u64): u64 {
  var current = 0u64;
  while (current < iterations) {
    current += 1u64;
  }
  return current;
}
